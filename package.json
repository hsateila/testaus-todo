{
  "name": "todo-training-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Simple Todo app (JS frontend+backend) using localStorage. Training base for Vitest and Cypress.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "cross-env NODE_OPTIONS=--no-experimental-webstorage vitest run",
    "test:watch": "cross-env NODE_OPTIONS=--no-experimental-webstorage vitest"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.9"
  }
}
